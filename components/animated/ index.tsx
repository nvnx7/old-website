import { Anchor, Box, Text } from 'grommet';
import { animated } from '@react-spring/web';

export const AnimatedBox = animated(Box);
export const AnimatedText = animated(Text);
export const AnimatedAnchor = animated(Anchor);
